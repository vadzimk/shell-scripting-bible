#!/bin/bash
# a comment line in a shell script is not processed by the shell
# however the first line is a special case 
# the poind sign and exclamation pint tells the shell what shell to run the script under.

