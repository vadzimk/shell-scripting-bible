#!/bin/bash
# look before you leap
jumb_directory=/home/vadzim
#
if [ -d $jumpa_directory ]
then echo "The $jump_directory directory exists."
	cd $jump_directory
	ls
else
	echo "The $jump_directory does not exist"
fi
